Run a quick health check on the project:

1. `npm run build` — Does it compile?
2. `npm run test` — Do tests pass?
3. `npm run lint` — Any lint errors?
4. `git status` — Any uncommitted changes?
5. Check for `TODO` and `FIXME` comments in src/

Report results as a status dashboard:
✅ Build: PASS/FAIL
✅ Tests: X/Y passing
✅ Lint: CLEAN/X errors
✅ Git: CLEAN/X uncommitted files
✅ TODOs: X found
